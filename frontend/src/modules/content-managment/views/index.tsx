import MainPanel from '@/shared/components/MainPanel'
import { strings } from '@/shared/locales'

const ContentManagment = () => {
    return (
        <MainPanel>
            <h1 className="title-01 font-bold">{strings.contentManagment.title}</h1>
        </MainPanel>
    )
}

export default ContentManagment
